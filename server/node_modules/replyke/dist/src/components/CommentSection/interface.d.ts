import { CommentElementsProps } from "../Comment";
import { FormStyleProps } from "../Form";
import { ApiPaths, Callbacks } from "../CommentSectionContainer/interface";
import { StatsBarStyleProps } from "../StatsBar";
import { Article, User } from "../interfaces";
export interface SectionStyleProps {
    commentChunkSize?: number;
    replyChunkSize?: number;
    loadMoreText?: string;
    loadMoreColor?: string;
    avatarPlaceholderColors?: string[];
}
export interface StyleConfig {
    sectionProps: SectionStyleProps;
    formProps: FormStyleProps;
    statsBarProps: StatsBarStyleProps;
    commentElementsProps: CommentElementsProps;
}
export interface CommentSectionProps {
    appKey?: string;
    articleId: string;
    article?: Article;
    setArticle: React.Dispatch<React.SetStateAction<Article | undefined>>;
    style: StyleConfig;
    apiBaseUrl: string;
    apiPaths?: ApiPaths;
    callbacks: Callbacks;
    currentUser?: User;
    dir?: "rtl" | "ltr";
}
