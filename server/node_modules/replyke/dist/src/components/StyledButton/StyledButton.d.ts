import { StyledButtonProps } from "./interface";
declare function StyledButton({ text, type, onClick, disabled, full, mt, buttonColor, borderRadius, fontSize, fontWeight, fontColor, paddingX, paddingY, shadow, }: StyledButtonProps): import("react/jsx-runtime").JSX.Element;
export default StyledButton;
